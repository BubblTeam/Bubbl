import React from 'react';
import { Provider } from 'react-redux';
import { render, screen, waitFor } from '@testing-library/react';

import MenuContainer from '../src/client/Containers/MenuContainer'

// describe('Unit testing React components', () => {
//   describe('MenuContainer', () => {
//     const props = {
//       statusMessage: 'What should Squirtle do?'
//     };

//     test('Renders the passed-in text with the label in bold', () => {
//       render(<MenuContainer statusMessage={props.statusMessage} />);
//       // expect(screen.getByText('Market ID:').nextSibling).toHaveTextContent(props.index.toString());
//     });
//   });
// });

describe('Menu Container tests', () => {
  


})